# npm registry configuration for Azure Artifacts.
#
# Usage (CI):
#   The npmAuthenticate@0 task auto-injects credentials.
#   Copy this file to frontend/.npmrc for CI.
#
# Usage (local):
#   1. Copy this file to frontend/.npmrc
#   2. Generate a PAT in Azure DevOps with Packaging (Read & Write) scope
#   3. Run: npx vsts-npm-auth -config .npmrc (or set token manually)
#
# NEVER commit .npmrc with real credentials.

registry=https://pkgs.dev.azure.com/YOUR_ORG/YOUR_PROJECT/_packaging/customer-registry-feed/npm/registry/
always-auth=true

; When using a PAT directly:
; //pkgs.dev.azure.com/YOUR_ORG/YOUR_PROJECT/_packaging/customer-registry-feed/npm/registry/:username=AzureDevOps
; //pkgs.dev.azure.com/YOUR_ORG/YOUR_PROJECT/_packaging/customer-registry-feed/npm/registry/:_password=${AZURE_ARTIFACTS_NPM_TOKEN}
; //pkgs.dev.azure.com/YOUR_ORG/YOUR_PROJECT/_packaging/customer-registry-feed/npm/registry/:email=not-used@example.com
